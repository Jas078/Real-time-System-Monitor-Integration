<Window x:Class="Real_time_System_Monitor_Integration.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Real_time_System_Monitor_Integration"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="20">

            <TextBlock Text="Real-time System Monitor"
               FontSize="24"
               FontWeight="Bold"
               Margin="0,0,0,20"/>

            <TextBlock x:Name="CpuLoadText" FontSize="16"/>
            <TextBlock x:Name="CpuTempText" FontSize="16"/>
            <TextBlock x:Name="GpuLoadText" FontSize="16"/>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <TextBlock Text="Refresh Interval (seconds): "
                           VerticalAlignment="Center" FontSize="14"/>
                <TextBox x:Name="IntervalTextBox"
                         Text="1"
                         Width="50"
                         FontSize="14"
                         VerticalAlignment="Center"/>
            </StackPanel>
            
            <Button x:Name="StartButton"
            Content="Start Monitoring"
            Click="StartButton_Click"
            Margin="0,20,0,0"/>

            <Button x:Name="StopButton"
            Content="Stop"
            Click="StopButton_Click"
            IsEnabled="False"
            Margin="0,20,0,0"/>

            <TextBlock x:Name="LastUpdateText"
            Text="Last Update:"
            FontSize="14"
            Margin="0,10,0,0"/>

        </StackPanel>
    </Grid>
</Window>
